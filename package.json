{
  "name": "root",
  "private": true,
  "scripts": {
    "runall": "lerna run --parallel start",
    "buildall": "bash ./build.sh",
    "prod": "node build/server.js"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
